---
name: product-copy
description: 为付费产品生成两类文案：GitHub README（说明书：非技术读者照着做就能跑通）与商品售卖页描述（营销：为什么需要、有什么特色）。当用户要写 README、产品说明书、详情页、售卖页、落地页文案，或要把一个项目包装成可售卖形态时使用。先建事实层再渲染，不用模板腔填空。
---

# 产品文案生成器

两类文案共享一个事实层，渲染规则完全隔离。

## 两类文案的本质

**README 是说明书。** 读者已经决定要用它了，只想跑通。写好的判据是：一个不写代码的普通人照着做，能不能成。说服、对比竞品、求 star 都不在这里。

**售卖页是营销。** 读者还没决定，默认动作是划走。写好的判据是：他能不能认出「这是给我的」，以及掏钱前那一刻的犹豫有没有被接住。

分开的理由：说明书靠承认边界建立信任，营销靠给足依据建立信任，语气和信息密度是反向的。混在一起会两头都不像，说明书里冒出促销腔，售卖页又克制得没人想买。所以每次只加载一个 profile。

共同前提：**默认读者技术水平为零。** 不假设读者知道终端、Node、包管理器、环境变量、clone 是什么。首次出现的技术名词就地解释。

## 铁律

1. **先建事实层，再写正文。** 跳过这一步直接写 = 失败。事实层默认只存在于上下文里，不落盘。
2. **宁缺不编，但缺口不进正文。** 没依据的地方不用形容词填，也不留 `[待确认]`。缺口按 core.md 的缺口协议处理，清单只在回复里给。
3. **不让用户去找素材。** 优先自动摄取（见 core.md 素材地图），提问上限 5 个，每个问题附默认假设。
4. **只加载一个 profile。** README 的语料不许带进售卖页，反之同理。
5. **依据嵌在叙述里，不做成证据清单。** 卖点要能被核对，但不为了显得具体去凑数、去另开一节「三大优势 + 逐条证据」。这条最容易被违反，因为「补齐结构」比「找到依据」容易。判定见 core.md。
6. **只交付用户要的东西。** 用户要 README 就只给 README。过程稿默认不落盘。摄取阶段默认不执行项目代码。

## 流程

### Step 0 · 判定要写哪一类

README（说明书）／售卖页（营销）／两类都要。

售卖页要再定载体，它决定长度和行动指令：电商详情页 / 独立落地页 / 社群短文案。

读者技术水平默认零基础。

始终加载 `references/core.md`（素材地图 + 事实层模板 + 通用质量规则 + persona 自检）。

再按类型加载**一个** profile：
- README → `references/profile-readme.md`
- 售卖页 → `references/profile-salespage.md`
- 两类都要 → 分两轮跑，第二轮先读第一轮的事实层，并做跨文档一致性检查

`references/humanizer-zh.md` 不在这里加载，它只在最后一步、且宿主没装 humanizer skill 时才读。

### Step 1 · 素材摄取（自动，不打扰用户）

按 core.md 的素材地图读取。**先读声明，再看代码；能不执行就不执行。**

摄取预算，防止把项目跑一遍：

| 动作 | 何时做 |
|---|---|
| 读 manifest / CI / `.env.example` / LICENSE / 入口文件 / 测试文件名 | 总是。这里能拿到 90% 的前置条件 |
| grep 错误信息字符串（`throw` / `raise` / `panic`） | 总是。报错表靠它，比通读源码便宜得多 |
| 读单个源文件全文 | 只在它是入口或该信息别处读不到时 |
| **执行命令**（跑测试、跑 CLI、启服务） | **默认不做。** 仅当声明缺失且该信息决定读者会不会卡住时，跑最小的那一条（如 `--version`、`init`），并说明为什么要跑 |
| 通读全部源码 | 永不 |

判据：能从声明读到的，不用运行验证。

产出两张清单，保持在上下文里，不写文件。事实清单记已确认、有出处的信息，出处标清楚（`来源: package.json`）。缺口清单记无法从材料推断、又影响文案质量的信息。

### Step 2 · 单关卡提问

只问缺口清单里**只有用户知道答案**的问题：

- 上限 5 个，一次问完，不做多轮追问
- 每个问题附带：`不回答我将按 X 假设处理`
- 禁止问能从材料读出来的（版本号、依赖、价格）
- 禁止问需要用户去做调研才能答的。「你的目标用户画像是什么」不行，「最近一次你自己用它是在干什么」可以

### Step 3 · 事实层（先于正文，默认不落盘）

按 core.md 模板在上下文里建好事实层。它的作用是约束下一步只能使用已确认的条目，不是交付物。

仅在这些情况写入 `<输出目录>/.product-copy/`：用户明确要求 / 要写第二类文案（下一轮要复用）/ 触发下方停下判定。

默认继续 Step 4，**但出现以下任一情况必须停下等确认**：
- 无依据支撑的卖点占了主卖点位，且换掉后没东西可写
- 定位句过不了反证测试
- 售卖页承诺在 README 里找不到兑现路径
- README 必需的前置条件无法确认，也没有通用做法可写（意味着读者一定卡住）

### Step 4 · 渲染正文

按 profile 渲染。此时不再发明新事实，只使用事实层里的条目。

### Step 5 · 去 AI 痕迹

正文成稿后整篇过一遍，这一步不能省，也不能只数一遍破折号就报通过。

按顺序找规则来源：

1. 宿主装了 `humanizer` skill → 调它。它的规则最细、会跟上游更新，本包就是为了配合它才把去痕迹留成独立一步
2. 没装 → 读 `references/humanizer-zh.md`，同源的随包副本
3. 两个都读不到 → 按 core.md 里的模式表自查，这是最弱的一档

不改的东西：路径、命令、报错原文、以及故意留作反例的句子。改完在回复里说清动了哪几处、哪些故意没动。

留意一个反向失误：去痕迹的时候别用「补一张对照表」「凑三条例子」来显得具体。整齐的结构本身就是 AI 味的来源之一。

### Step 6 · 自检 + 报告

跑 core.md 的通用规则、profile 内的规则，再用 persona 过一遍。

**报告只说未通过的。** 默认在回复里给三行以内：未通过的点 + 位置 + 补什么能修好，再加缺口清单（`缺什么 | 补了能修好什么`）。通过的不逐条列举，不写成表格，不落盘。用户明确要完整自检报告时才写 `review.md`。

## 输出物

**默认只交付一个文件：正文。**

| 文件 | 何时产出 | 位置 |
|---|---|---|
| `README.md` / `salespage.md` | 总是 | 见下方路径规则 |
| `facts.md` | 用户要写第二类文案，或明确要求 | `<输出目录>/.product-copy/` |
| `review.md` | 用户明确要求 | 同上 |

### 路径规则

优先级从高到低：

1. 用户在指令里指定的位置（"写到 xxx"），直接用
2. 输出目录下的 `.product-copy/config.json`（若存在）：
   ```json
   { "outputDir": "~/Desktop/copy-drafts", "keepIntermediates": true }
   ```
3. 默认：
   - README 且目标是一个项目目录 → 写进项目根。**该位置已有 README.md 时不覆盖**，写成 `README.draft.md` 并在回复里说明
   - 售卖页，或没有明确项目目录 → `~/Desktop/<产品名>-copy/`

写入前若发现目标文件已存在且内容与本次产出无关，停下问，不静默覆盖。

