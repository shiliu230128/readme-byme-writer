---
name: product-copy
description: 为付费产品生成两类文案：GitHub README（面向非技术读者：可作为已购买者的上手交付文档，也可作为公开仓库的说服文档）与商品售卖页描述。当用户要写 README、产品说明书、详情页、售卖页、落地页文案，或要把一个项目包装成可售卖形态时使用。先建事实层再渲染，不用模板腔填空。
---

# 产品文案生成器

两类文案共享一个事实层，profile 完全隔离渲染。

## 前提设定（本 skill 与通用 README 写法的关键区别）

这里的 GitHub 有两种用法，读者都**不假设懂技术**：

1. **交付载体**：项目卖出后把仓库发给买家 → 读者是已付费、只想跑通的人
2. **公开展示**：任何人都能看到 README，决定要不要用、给不给 star、要不要买 → 读者是未决策的路过者

两种读者的信任机制不同，所以 README profile 里分了 `交付型` / `公开型` / `双重` 三种模式。混写会双向损伤：未决策者被安装细节赶走，已付费者在说服话术里找不到第一步。

共同的硬约束：**默认读者技术水平 = 零。** 不假设读者知道终端、Node、包管理器、环境变量、clone 是什么。首次出现的技术名词必须就地解释。

## 铁律（违反任何一条即为失败）

1. **先建事实层，再写正文。** 跳过 Step 3 直接写正文 = 失败。但事实层默认只存在于上下文里，不落盘（落盘条件见 Step 3）。
2. **宁缺不编，但缺口不进正文。** 无依据处禁止用形容词填空，也禁止在正文留 `[待确认]`。正文是给读者看的成品。缺口按 C4 分类处理（不写 / 改成通用做法 / 等用户补），清单只在回复里给。
3. **不让用户去找素材。** 优先自动摄取（见 `references/core.md` 素材地图），提问上限 5 个，每个问题附默认假设。
4. **profile 隔离。** Step 0 判定类型后只加载一个 profile。禁止把售卖页语料带进 README，或反之。README 靠可验证与自我限缩建立信任，售卖页靠证据堆叠与风险消解，方向相反。平均化 = 生出带营销腔的 README 和太克制的售卖页。
5. **所有质量判定用 `references/core.md` 的算子执行**，不凭感觉判断"写得挺好"。
6. **只交付用户要的东西。** 用户要 README 就只给 README。过程稿（事实层、对账表、自检报告）默认不落盘、不在回复里展开。摄取阶段默认不执行项目代码（预算见 Step 1）。

## 流程

### Step 0 · 判定类型与模式（不提问，除非无法推断）

先判要写哪一类，再定模式：

**README 模式**（三选一，默认 `双重`）
- `交付型` — 私有仓库或只发给买家。不写差异化、不写 star 请求，首屏归"跑通"
- `公开型` — 免费开源或引流。首屏归"说服"，必须有效果图与替代方案对账
- `双重` — 公开仓库 + 售卖。按 R8 硬分界线分层，上说服下交付

**售卖页载体**（决定长度与行动指令）
- `电商详情页` / `独立落地页` / `社群短文案`

**读者技术水平**：`零基础`（默认）/ `会复制粘贴` / `开发者`

始终加载 `references/core.md`（素材地图 + 事实层模板 + 质量算子 C1–C8 + persona）。

再按类型加载**一个** profile：
- README → `references/profile-readme.md`
- 售卖页 → `references/profile-salespage.md`
- 两类都要 → 分两轮跑；第二轮先读第一轮的事实层，并执行跨文档一致性检查（C7）

`references/humanizer-zh.md` 不在这里加载。它只在 Step 5 的 C8 那一步、且宿主没装 `humanizer` skill 时才读。

### Step 1 · 素材摄取（自动，不打扰用户）

按 `core.md` 的素材地图读取。**先读声明，再看代码；能不执行就不执行。**

摄取预算（防止把项目跑一遍）：

| 动作 | 何时做 |
|---|---|
| 读 manifest / CI / `.env.example` / LICENSE / 入口文件 / 测试文件名 | 总是。这里能拿到 90% 的前置条件 |
| grep 错误信息字符串（`throw` / `raise` / `panic`） | 总是。报错表靠它，比通读源码便宜得多 |
| 读单个源文件全文 | 只在它是入口或该信息别处读不到时 |
| **执行命令**（跑测试、跑 CLI、启服务） | **默认不做。** 仅当声明缺失且该信息决定读者会不会卡住时，跑最小的那一条（如 `--version`、`init`），并说明为什么要跑 |
| 通读全部源码 | 永不 |

判据：**能从声明读到的，不用运行验证。** 「44 个测试通过」这类数字属于加分证据，不是必需项。除非用户要的是公开型 README 且缺可信信号，否则不值得为它跑一遍测试。

产出两张清单（保持在上下文里，不写文件）：
- **事实清单**：已确认、有出处的信息（标注出处，如 `来源: package.json`）
- **缺口清单**：无法从材料推断、且影响文案质量的信息

### Step 2 · 单关卡提问（唯一需要用户参与的步骤）

只问缺口清单里**只有用户知道答案**的问题：

- 上限 5 个，一次问完，不做多轮追问
- 每个问题附带：`不回答我将按 X 假设处理并在正文标注`
- 禁止问能从材料读出来的（版本号、依赖、价格）
- 禁止问需要用户去做调研才能答的
  - ❌「你的目标用户画像是什么」
  - ✅「最近一次你自己用它，是在干什么」

### Step 3 · 事实层 + 对账表（先于正文，默认不落盘）

按 core.md 模板在上下文里建好事实层与对账表，**默认不写文件**。它的作用是约束 Step 4 只能使用已确认的条目，不是交付物。

仅在这些情况写入 `<输出目录>/.product-copy/`：用户明确要求 / 要写第二类文案（下一轮要复用）/ 触发下方停下判定。

默认继续 Step 4，**但出现以下任一情况必须停下等确认**：
- 无证据支撑的卖点 ≥ 3 条
- 定位句未通过反证测试（C1）
- 售卖页承诺在 README 中找不到兑现路径
- README 必需的前置条件无法确认，且没有通用做法可写（意味着读者一定卡住）

### Step 4 · 渲染正文

按 profile 渲染。此时不再发明新事实，只使用事实层里的条目。

### Step 5 · 自检 + 报告

逐条执行 core.md 的 C1–C8、profile 内的 P*/R*/S* 规则，再跑五 persona 对抗。

**最后一道是 C8 去 AI 痕迹**：正文成稿后必须整篇过一遍，检查中文里那些机器味模式（三段式、内联加粗列表、破折号成串、句长均一等）。这一步要真的改写，不是数一遍破折号个数就报通过。

规则按这个顺序取：宿主装了 `humanizer` skill 就调它（规则最细，会跟上游更新）；没装就读 `references/humanizer-zh.md`，那是随包副本；两个都没有才退回 C8 的表格自查。改完在回复里说清动了哪几处、哪些地方故意没动。

**报告只说未通过的。** 默认在回复里给三行以内：未通过的规则 + 位置 + 补什么能修好，再加缺口清单（`缺什么 | 补了能修好什么`）。通过的规则不逐条列举，不写成表格，不落盘。用户明确要完整自检报告时才写 `review.md`。

## 输出物（默认只给正文，其余按需）

**默认只交付一个文件：正文。** 中间产物是过程稿，用户没要就不占他的桌面。

| 文件 | 何时产出 | 位置 |
|---|---|---|
| `README.md` / `salespage.md` | 总是 | 见下方路径规则 |
| `facts.md` | 用户要写第二类文案，或明确要求 | `<输出目录>/.product-copy/` |
| `ledger.md` | 用户明确要求，或 Step 3 触发停下判定 | 同上 |
| `review.md` | 用户明确要求 | 同上 |

未产出 review.md 时，自检结果**不写成文件**，只在回复里给一段：未通过的规则 + 缺口清单。通过的规则不逐条汇报。

### 路径规则（可配置）

优先级从高到低：

1. 用户在指令里指定的位置（"写到 xxx"），直接用
2. 输出目录下的 `.product-copy/config.json`（若存在）：
   ```json
   { "outputDir": "~/Desktop/copy-drafts", "keepIntermediates": true }
   ```
3. 默认：
   - README 且目标是一个项目目录 → 写进项目根。**该位置已有 README.md 时不覆盖**，写成 `README.draft.md` 并在回复里说明，让用户自己替换
   - 售卖页，或没有明确项目目录 → `~/Desktop/<产品名>-copy/`

写入前若发现目标文件已存在且内容与本次产出无关，停下问，不静默覆盖。

